# Eval Task

/revenue-recognition @input contains the source files for period 202605. Please process the dine-in revenue rollup.

## Requirements

- Run validate and preview only.
- Do not run upload.
- Treat `input/` as the only source-data directory.
- Write the preview artifact under `output/`.
- In the final answer, state whether validate passed, whether preview generated an artifact, and the artifact path.

## Boundaries

Do not read the original source-code repository, do not use the host user home directory, and do not treat historical preview files as this run's result.